# Archive Center 3.0 Go 대형 파일 분리 계획

작성일: 2026-07-11

## 목적

현재 동작을 바꾸지 않고 비대한 Go 파일을 같은 패키지 안의 책임별 파일로 나눈다.

이번 단계에서 하지 않는 것:

- HTTP 경로, 요청·응답 JSON, SQL, DB 스키마 변경
- 함수명·시그니처 변경
- 새 인터페이스나 공통 추상화 도입
- `httpapi`와 `store`를 새 하위 패키지로 이동
- JS 로직 이전이나 기능 개선을 파일 이동과 동시에 수행
- 테스트 기대값 변경

분리 후에도 패키지는 그대로 유지한다.

- `group_turn.go`, `group_narrative.go`, `turn_extraction.go` → `package httpapi`
- `mariadb.go` → `package store`

같은 패키지 안의 파일 이동은 기존 비공개 함수와 타입을 그대로 공유할 수 있으므로 첫 단계에서 순환 의존 위험이 가장 낮다.

## 현재 크기

| 파일 | 줄 수 | 함수 수 | 주요 문제 |
|---|---:|---:|---|
| `internal/httpapi/group_turn.go` | 약 16,700 | 505 | 완료 저장, prepare-turn, 기억 검색·주입, 롤백, 응답 모델, 과거 SEQ 계약 표면이 혼재 |
| `internal/httpapi/group_narrative.go` | 약 11,550 | 389 | 세션, 스토리라인, 인물, 세계 규칙, 에피소드, 메트릭, 피드백, import가 한 파일에 혼재 |
| `internal/store/mariadb.go` | 약 6,700 | 240 | 모든 MariaDB 저장 영역과 삭제·이전 로직이 한 구현 파일에 집중 |
| `internal/httpapi/turn_extraction.go` | 약 5,670 | 194 | LLM 호출, JSON 복구, 비밀·별칭, 파생 저장, 상태·벡터 처리가 결합 |

테스트도 함께 비대하다.

| 파일 | 줄 수 | 테스트 수 |
|---|---:|---:|
| `group_turn_test.go` | 약 13,860 | 202 |
| `group_narrative_test.go` | 약 6,950 | 98 |

## 1. `group_turn.go` 분리

첫 이동은 함수 내부를 고치지 않고 아래 앵커 단위로 통째로 옮긴다. 줄 번호보다 함수명과 섹션 주석을 기준으로 삼는다.

| 새 파일 | 이동할 책임 | 주요 앵커 |
|---|---|---|
| `group_turn_routes.go` | 턴 라우트 등록과 구형 턴 호환 핸들러 | `registerTurnRoutes`, `handleTurns`, `handleTurnsRepairReplay`, `handleTurnsComplete` |
| `group_turn_complete.go` | `/complete-turn` 실행과 이미 저장된 pair 판정, 유지보수 handoff | `handleCompleteTurn`, `handleCompleteTurnDecoded`, `completeTurnEpisodeCheckpoint`, `completeTurnAlreadyPersistedWithContent` |
| `group_turn_prepare.go` | `/prepare-turn`, `/effective-inputs` 최상위 조립 | `handlePrepareTurn`, `handleEffectiveInputs` |
| `group_turn_rollback.go` | `/rollback` 범위 계산과 벡터 ID 계산 | `handleRollback`, `rollbackVectorDocumentIDs`, `rollbackVectorDocumentAlias` |
| `prepare_turn_planner.go` | 감독관 입력, 약한 입력 계획, 실행 계약, 진행 선택 | `prepareTurnEvidenceCounts`, `buildSupervisorInputPack`, `buildWeakInputPlannerContract`, `buildPlannerExecutionContract`, `buildProgressionChoiceLedger` |
| `prepare_turn_render.go` | 주입 pack, Effective Input, 투명성 표시 모델 | `prepareTurnInjectionBlock`, `buildInjectionPack`, `buildPrepareTurnInputTransparencyRenderModel`, `buildPrepareTurnEffectiveInputPreview` |
| `prepare_turn_vector.go` | Chroma 검색·hydrate·카운터 | `prepareTurnVectorShadow`, `prepareTurnVectorMemoryHydration`, `prepareTurnHydrateVectorMemoryHits`, `prepareTurnHydrateVectorArtifactHits` |
| `prepare_turn_memory.go` | 기억 lane 선택·collapse·비밀/정체성 보호·회상문 | `prepareTurnMemoryLaneSelection`, `selectPrepareTurnMemoryLanes`, `filterPrepareTurnProtectedMemoryLaneSelection`, `buildPersonaRecollectionText`, `buildCharacterPrivateRecollectionText` |
| `prepare_turn_assembly.go` | 최종 주입 조립과 문자 예산 | `buildPrepareTurnInjectionAssembly`, `buildBudgetDecisions`, `addPrepareTurnBlock`, `buildInputContextText` |
| `group_turn_response_models.go` | recall/session/control/continuity/progression 및 writeback 표시 모델 | `buildRecallResult`, `buildSessionState`, `buildNarrativeControl`, `buildContinuityPack`, `buildProgressionLedger`, `buildInputTransparency`, `buildRollbackPlan` |
| `group_turn_contract_seq165_168.go` | Step 16.5·16.8 검사/계약 표면 | `buildStep165...`, `buildStep168...` |
| `group_turn_contract_seq16.go` | SEQ-16 retrieval/authority/temporal 계약 표면 | `buildRetrievalRoleBoundary`부터 SEQ-16 replay·index 표면까지 |
| `group_turn_contract_seq17.go` | SEQ-17 평가·배포·Chroma migration 표면 | `buildStep17...` |
| `group_turn_contract_seq18.go` | SEQ-18 VR/HY/QR/VX 및 pre-release 표면 | SEQ-18 섹션 주석 이후의 builder 전체 |

### `group_turn.go` 1차 목표

- 먼저 SEQ-16.5~18 builder를 옮겨 런타임 핵심과 과거 검증 표면을 분리한다.
- 그 다음 rollback, render, vector, memory처럼 경계가 분명한 묶음을 옮긴다.
- 마지막에 `handlePrepareTurn`과 `handleCompleteTurnDecoded`를 별도 파일로 옮긴다.
- 두 대형 핸들러의 함수 내부 분해는 별도 작업으로 남긴다. 파일 이동과 로직 리팩터링을 동시에 하지 않는다.

## 2. `group_narrative.go` 분리

| 새 파일 | 이동할 책임 | 주요 앵커 |
|---|---|---|
| `group_narrative_routes.go` | narrative 계열 라우트 등록만 | `registerNarrativeRoutes` |
| `group_sessions.go` | 세션 목록·export·resume·state·continuity·active scope | `handleSessionsList`부터 `handleNarrativeControlGet`, 세션 patch/delete |
| `group_storylines.go` | 스토리라인 조회·수정·신뢰·동기화와 선택 helper | `handleStorylinesGet`, `handleStorylinePatch`, `handleStorylinesSync` |
| `group_characters.go` | 인물 목록·상세·사건·상태 이력·수정·삭제 | `handleCharactersGet`부터 character state patch 계열 |
| `group_world_rules.go` | 세계 규칙 조회·상속·동기화·수정·삭제 | `handleWorldRulesGet`부터 `handleWorldRuleDelete` |
| `group_episodes.go` | 에피소드·챕터·아크·사가 생성/검색/수정과 pending thread | `handleEpisodesGet`부터 `handlePendingThreadDelete` |
| `group_narrative_metrics.go` | LC/TM 메트릭과 `narrativeEvidence` 수집 | `handleMetricsLC1C`부터 `handleMetricsTM1D` |
| `group_audit_feedback_import.go` | audit, feedback, HypaMemory import | `handleAuditGet`, `handleFeedbackPost`, `handleImportHypamemory` |

스토리라인 선택 타입인 `storylineSupervisorSelection`은 `group_storylines.go`에 두되, `group_turn`에서 계속 같은 패키지 내부 타입으로 사용한다.

## 3. `mariadb.go` 분리

`mariadbStore` 구조체와 연결 관리는 `mariadb_core.go`에 남기고, 메서드 receiver는 도메인별 파일로 옮긴다.

| 새 파일 | 이동할 책임 | 대표 메서드 |
|---|---|---|
| `mariadb_core.go` | `mariadbStore`, 연결, ping, reset, 공통 query helper | `OpenMariaDB`, `Close`, `ensureDB`, `Ping`, `ResetAll` |
| `mariadb_chat_memory.go` | chat log, Effective Input, memory, evidence, KG, audit, supersession | `SaveChatLog`, `ListMemories`, `SaveEvidence`, `SaveKGTriple`, `SaveAuditLog` |
| `mariadb_character_feedback.go` | critic feedback, character event, entity, trust | `SaveCriticFeedback`, `SaveCharacterEvent`, `SaveEntity`, `SaveTrust` |
| `mariadb_session_hierarchy.go` | 세션 목록, resume pack, chapter/arc/saga | `ListSessions`, `GetResumePack`, `SaveChapterSummary`, `SaveArcSummary`, `SaveSagaDigest` |
| `mariadb_story_world.go` | storyline, world rule | `SaveStoryline`, `ListStorylines`, `SaveWorldRule`, `ListWorldRules` |
| `mariadb_runtime_state.go` | active scope, character/current state, pending thread, canonical state, episode | `UpsertActiveScope`, `SaveCharacterState`, `SavePendingThread`, `SaveActiveState`, `SaveCanonicalStateLayer`, `SaveEpisodeSummary` |
| `mariadb_migration.go` | 세션 copy/move/lock/rollback/cleanup | `CompleteSessionMigration`, `LockSessionMigrationSource`, `RollbackSessionMigration`, `CleanupSessionMigrationSource` |
| `mariadb_delete.go` | 턴·세션별 일괄 삭제 메서드 | `DeleteChatLogs`부터 각 `Delete...` 메서드 |
| `mariadb_persona.go` | persona capsule, attachment, subjective entity memory | `CreatePersonaMemoryCapsule`, `AttachPersonaMemoryCapsule`, `CreateProtagonistEntityMemory` |
| `mariadb_narrative_state.go` | consequence, psychology, fork lineage, offscreen carry, capture verification | `SaveConsequenceRecord`, `SavePsychologyBranch`, `SaveForkLineageRecord`, `SaveThemeOffscreenCarry` |
| `mariadb_status.go` | 상태 스키마·현재값·변경 이벤트·효과 | `SaveStatusSchemaProposal`, `SaveStatusSchemaDefinitions`, `SaveStatusCurrentValue`, `SaveStatusChangeEvent`, `SaveStatusEffect` |

인터페이스 구현 확인용 `var _ ... = (*mariadbStore)(nil)` 선언은 해당 기능 파일 가까이에 둔다.

## 4. `turn_extraction.go` 분리

| 새 파일 | 이동할 책임 | 주요 앵커 |
|---|---|---|
| `turn_extraction_types.go` | LLM/embedding 설정, 결과 타입, saver 인터페이스 | `completeTurnLLMConfig`, `completeTurnEmbeddingConfig`, `artifactSaveResult` |
| `turn_extraction_policy.go` | 충돌·retention·OOC·source control·입력 정리 | `classifyConflict`, `applyRetentionPolicy`, `shouldApplyCompleteTurnOOCGuard`, `sanitizeTextForCriticInput` |
| `turn_extraction_alias.go` | 인물 이름 표준화·한글/가타카나 alias | `canonicalCharacterName`, `normalizeCharacterKey`, `normalizeKatakanaMixedKey` |
| `turn_extraction_config.go` | UI/runtime/env LLM·embedding 설정 선택과 trace | `completeTurnExtractionConfigFromMeta`, `selectCompleteTurnLLMCoreConfig`, `selectCompleteTurnEmbeddingConfig` |
| `turn_extraction_critic.go` | critic 호출, prompt, world-rule audit, JSON 파싱·복구·정규화 | `runCompleteTurnCritic`, `runCompleteTurnWorldRuleAudit`, `buildCompleteTurnCriticPrompt`, `parseJSONFromLLMContent` |
| `turn_extraction_private.go` | protected secret, identity, subjective memory, persona 후보 | `normalizeSubjectiveEntityMemories`, `normalizeProtectedSecrets`, `canonicalSubjectiveEntityOwner`, `saveSubjectiveEntityMemoriesFromExtraction` |
| `turn_extraction_persist.go` | 파생 결과 저장 orchestration, 중복 억제, soft prune | `saveCriticExtractionArtifacts`, `memoryForTurnAlreadyExists`, `mergeSimilarMemoryInsteadOfInsert`, `applyCriticSoftPrune` |
| `turn_extraction_conditions.go` | 신체·개체 상태와 지속 효과 | `savePhysicalConditionsFromExtraction`, `saveEntityConditionsFromExtraction`, `saveConditionEffectsFromExtraction` |
| `turn_extraction_character_state.go` | 인물·관계·현재 상태·canonical layer 저장 | `saveCharacterAndStateArtifacts`, character state merge helper |
| `turn_extraction_vector.go` | memory/evidence/world-rule embedding과 upsert | `upsertMemoryVector`, `upsertDerivedArtifactVector`, `callEmbedding`, provider별 embedding 호출 |
| `turn_extraction_helpers.go` | 범용 변환·정규화 helper | `mapFromAny`, `sliceFromAny`, 숫자 변환, KG·관계·world-state helper |

## 테스트 분리

생산 코드 이동과 같은 커밋에서 테스트 내용을 고치지 않는다. 전체 생산 파일 이동이 끝난 뒤 테스트 파일만 다음처럼 옮긴다.

- `group_turn_test.go` → `group_turn_complete_test.go`, `group_turn_prepare_test.go`, `group_turn_rollback_test.go`, `prepare_turn_memory_test.go`, `prepare_turn_render_test.go`
- `group_narrative_test.go` → sessions/storylines/characters/world-rules/episodes/metrics 기준 파일
- 기존 SEQ 테스트 파일은 그대로 유지
- `turn_extraction_test.go`는 현재 289줄이므로 당장은 유지
- MariaDB 테스트는 이미 기능별 파일이 있으므로 새 테스트를 기존 도메인 파일명에 맞춰 추가

테스트 이동 시에도 assertion, fixture, 테스트 이름은 변경하지 않는다.

## 권장 실행 순서

1. 현재 1~5 안정화 변경을 GitHub push 없이 로컬 체크포인트로 고정
2. `group_turn.go`의 SEQ-16.5~18 builder만 기계적으로 이동
3. 전체 `gofmt`, `go vet ./...`, `go test ./... -count=1`
4. `group_narrative.go`를 라우트 도메인별로 이동 후 같은 검증
5. `mariadb.go`를 receiver 도메인별로 이동 후 MariaDB·rollback·migration 테스트 집중 실행
6. `turn_extraction.go`를 policy/config/critic/persist/vector 경계로 이동 후 같은 검증
7. `group_turn.go`의 runtime prepare/complete/rollback 묶음 이동
8. 대형 테스트 파일 이동
9. 파일별 줄 수와 함수 수를 다시 측정하고 문서 갱신

## 각 이동 단위의 필수 검증

매 이동마다 다음을 모두 통과해야 다음 묶음으로 진행한다.

```text
gofmt -l <이동한 Go 파일>
go vet ./...
go test ./... -count=1
```

추가 회귀 확인:

- `POST /prepare-turn` 응답 JSON 동일
- `POST /complete-turn` raw/derived/vector lane 동일
- 중복 pair와 idempotency 동작 동일
- `DELETE /rollback/{turn}` 범위 동일
- copy/move/lock/rollback SQL 결과 동일
- critic/embedding 설정 선택 source 동일
- Chroma upsert/search/delete 통합 테스트 동일

## 중단 조건

다음 중 하나라도 발생하면 다음 파일로 넘어가지 않는다.

- route 또는 JSON 필드 변화
- SQL 문자열·트랜잭션 범위 변화
- import cycle을 피하려고 새 인터페이스가 필요한 상황
- 기존 테스트 기대값을 바꿔야만 통과하는 상황
- 파일 이동과 무관한 기능 수정 필요 발견

이 경우 기능 수정은 별도 작업으로 기록하고, 파일 분리 diff에는 포함하지 않는다.
