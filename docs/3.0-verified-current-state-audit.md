# Archive Center 3.0 현재 상태 코드 감사

작성일: 2026-07-11

## 2026-07-12 구조 이전 완료 상태

아래 최초 감사의 작업 목록 1~9는 현재 코드와 로컬 커밋에서 완료됐다.

- 전체 회귀 테스트 복구, 저장성 POST 멱등성, Go 1.26.5 CI, ChromaDB
  1.5.9 `/api/v2`, `model` 오분류 축소를 완료했다.
- 대형 Go 파일은 같은 패키지 안에서 동작 변경 없이 분리했고 backend
  timing을 추가했다.
- prepare-turn이 이미 반환한 세션 자료를 JS가 다시 읽는 중복 경로를
  줄였다.
- 대시보드는 `dashboard.viewmodel.v1`, 타임라인과 탐색은
  `presentation.viewmodel.v1`을 백엔드 기준 표시 모델로 사용한다.
- 삭제·리롤의 실제 DB 범위와 복사·연결·이동 이후 턴 계산은 백엔드가
  결정한다. JS는 RisuAI 채팅 변화를 관찰하고 백엔드의 일회용 삭제 결정
  토큰을 적용한다.

관련 구현 기록은 `3.0-js-backend-duplicate-computation-removal.md`와
`3.0-go-file-decomposition-plan.md`를 따른다. 실제 장기 세션을 이용한
삭제·리롤·현재 상태·인물 인식 검증은 3.0 기능 구현이 끝난 뒤 안정화
단계에서 함께 진행한다.

## 2026-07-11 현재 상태 보정

아래 최초 감사에서 결함으로 기록한 전체 테스트, 저장성 POST 멱등성,
Go/CI, ChromaDB 기준, `model` 재분류 문제는 이후 커밋에서 해결됐다.
대형 Go 파일 분리, backend timing, 안전한 prepare-turn 세션 조회 재사용도
완료됐다. 기존 정상 동작의 회귀 기준은
`3.0-core-regression-suite.md`와 `testdata/core-regression-suite.json`에
격리된 실행 묶음으로 고정했다. 아래의 최초 실패 수와 작업 목록은 발견
당시 기록이며 현재 미완료 목록으로 사용하지 않는다.

## 2026-07-11 안정화 1~5 재검증 결과

이번 변경은 3.0 백엔드 이전 자체가 아니라, 이전 작업을 시작하기 전에 필요한 기존 동작 안정화다.

1. **전체 Go 테스트 복구**
   - 고정된 개인 PC 경로를 임시 fixture로 교체했다.
   - 과거 JS 문자열만 찾던 검사를 현재 공개 동작 계약에 맞췄다.
   - 기억 재생성 응답은 `code`와 사람이 읽는 `detail`을 분리했다.
   - `go test ./... -count=1` 전체 통과를 출구 기준으로 사용한다.
2. **저장성 POST 중복 방지**
   - `/turns`, `/turns/complete`, `/effective-inputs`, `/complete-turn`은 JS에서 자동 즉시 재시도하지 않는다.
   - `/complete-turn`은 같은 세션·턴·입출력으로 안정적인 멱등성 키를 만든다.
   - 백엔드는 처리 중 요청을 합치고, JS 대기열은 `/complete-turn/request-status` 확인 후에만 복구한다.
   - 상태 조회는 단순 완료와 HTTP 성공을 구분한다.
3. **Go 1.26.5 이상 및 CI**
   - `go.mod` 최소 버전을 1.26.5로 올렸다.
   - 패키징은 Go 1.26.5 이상을 허용한다.
   - CI는 JS 문법, gofmt, vet, 전체 테스트, 빌드, govulncheck를 실행한다.
4. **ChromaDB 1.5.9 및 API v2 통일**
   - Windows/POSIX/Termux 설치 기준을 Python 패키지 `chromadb==1.5.9`와 `/api/v2`로 통일했다.
   - 백엔드는 시작 전에 heartbeat와 컬렉션 API를 검사한다.
   - `/ready`도 실행 중 Chroma 연결을 다시 검사한다.
   - CI에서 실제 ChromaDB 1.5.9를 실행해 생성·upsert·검색·삭제를 검증한다.
5. **`model` 내부 재분류 축소**
   - RisuAI의 non-model 타입 제외는 그대로 유지한다.
   - `model` 요청은 프롬프트 마커나 active-tail 불일치만으로 차단하지 않는다.
   - 이미 출력된 초안을 후처리 결과로 교체하는 `post_output_secondary_request` 보호는 유지한다.

주의: 삭제·리롤·복사·연결·이동, 번역·후처리 canonical 선택, 기존 기억 주입 순서는 이번 작업에서 재작성하지 않았다.

기준:

- 기준 커밋: `26818fe` (`release-2.5.0-rc4` 병합)
- 검사 대상: 기준 커밋 위의 현재 `source` 미커밋 변경 포함
- 사용자 실사용 확인과 코드/자동 테스트 확인을 구분한다.
- 정상 기능을 3.0 신규 구현 과제로 다시 적지 않는다.

## 검증 결과 요약

| 항목 | 현재 판정 | 근거 | 3.0 처리 |
|---|---|---|---|
| RisuAI 요청 타입 게이트 | 부분 구현, 잔여 위험 있음 | `isNarrativeType`, `isSaveType`, `isContextInjectionType`은 `model`만 허용한다. 그러나 이후 `buildMainRequestOwnershipDecision`이 payload/active tail/마커를 다시 검사해 `model`도 차단할 수 있다. | 타입 게이트는 유지한다. 내용 기반 재분류는 신규 백엔드 기능으로 확대하지 말고 실제 오판 사례와 함께 축소/검토한다. |
| 실제 유저 입력 선택 | 현재 동작 및 실사용 확인 | active chat, input hook, payload 후보와 입력 개선 허용 경로가 구현되어 있다. | 신규 기능이 아니다. 구조 이동 시 보호할 회귀 항목이다. |
| 기억 주입과 Effective Input | 구현됨, 백엔드/JS 혼합 소유 | 백엔드가 `injection_pack`, `input_transparency_model`, `effective_input_preview`를 반환하고 UI가 표시한다. 현재 최종 글자 예산 적용은 JS가 소유한다. | 기능 재설계 대상이 아니다. 3.0에서는 이미 있는 백엔드 모델과 JS 최종 적용의 경계만 정리한다. |
| 턴 raw/derived/vector 저장 | 구현됨, 실사용 확인 | `/complete-turn`과 persistence lane이 동작한다. 관련 내부 테스트가 통과한다. | 신규 구현 대상이 아니다. timeout 동시 중복만 별도 결함으로 수정한다. |
| 완료된 pair 중복 저장 방지 | 구현됨 | 동일 턴, 동일 user+assistant pair, 후처리 replay 중복 방지 테스트가 존재하고 통과한다. | 유지한다. `request_id` 멱등성은 처리 중인 요청의 동시 중복을 보강하는 별도 작업이다. |
| 삭제·리롤 | 구현됨, 실사용 확인 | backend rollback clamp, `protected_before_turn`, `min_from_turn` 및 관련 테스트가 존재한다. | 기능 재작성 대상이 아니다. JS 자동 감지와 backend 삭제 범위 계산의 중복 소유만 추후 줄인다. |
| 복사·연결·이동 | 구현됨, 실사용 확인 | migration preview/complete/reindex, routing baseline 및 backend migration 테스트가 존재한다. | 신규 구현 대상이 아니다. 현재 기준 턴 보호를 유지하며 UI 계산 중복만 정리한다. |
| 번역·GigaTrans | 구현됨 | GigaTrans canonical 추출, trace 필드, malformed 보존 경로가 JS에 있다. | 신규 기능이 아니다. 백엔드 이전은 선택적 구조 정리이며 동작 변경을 동반하면 안 된다. |
| 일반 후처리 최종 출력 | 구현됨, 실사용 확인 | postprocessor canonicalization과 후처리 replay 중복 방지 테스트가 존재한다. | 신규 기능이 아니다. 실제 화면 최종값 확인은 계속 JS가 맡는다. |
| 현재 상태·인물별 인식 보정 | 코드 및 내부 테스트 구현 완료 | `narrative_state_contract.go`와 테스트가 존재하며 `go test ./internal/...`가 통과한다. | 장기 실사용 검증 전이다. 3.0 후보 기능으로 유지하되 배포 완료로 보지 않는다. |
| Alias Repair | 구현됨 | dry-run/apply, 확정 근거가 없는 역할·공개범위 분리 자동 병합 금지, 본문 미변경 정책과 테스트가 있다. | 신규 구현이 아니다. 실사용 오탐 자료가 생길 때 보수적으로 보강한다. |
| 원작 자료 전용 DB | 저장 기반 구현, 런타임 미완료 | 작품·연속성·문서·시점·개체·claim·세션 연결 테이블과 Store CRUD를 추가했다. | 자동 추출, 검수 API/UI, 벡터 검색, prepare-turn 주입은 `3.0-reference-work-library-and-session-binding-design.md` 계약에 따라 구현한다. |

## 발견 당시 결함 기록

이 절의 1~5는 최초 감사 당시의 원인 기록이다. 현재 미완료 목록이
아니며 위의 2026-07-11 및 2026-07-12 보정 내용이 현재 상태다.

### 1. 저장성 POST의 처리 중 중복 가능성

`R.nativeFetch` 경로는 `Promise.race`로 응답 대기만 끝내며 실제 요청을 취소하지 않는다. timeout timer도 해제하지 않는다. `/complete-turn`은 write-ahead queue와 retry 경로가 있어 첫 요청이 아직 처리 중일 때 같은 POST가 다시 들어갈 수 있다.

기존 pair 중복 방지는 완료된 저장을 다시 넣는 상황에는 유효하지만 동시 처리 중 중복을 완전히 막지는 않는다.

필요 작업:

- 저장성 POST 자동 즉시 재시도 금지
- 안정적인 idempotency key
- backend 처리 상태 ledger
- timeout 후 상태 조회
- timer 정리

### 2. `model` 내부 내용 기반 재분류 잔존

최상위 타입 게이트는 `model`만 허용하지만 이후 다음 조건으로 다시 차단할 수 있다.

- payload user tail 없음
- post-output secondary request 판정
- auxiliary marker
- payload/active tail mismatch

이는 과거 `auxiliary_module_request` 오판과 같은 계열의 위험이다. 3.0에서 이 분석기를 더 크게 백엔드로 옮기는 것은 현재 목표와 맞지 않는다. 먼저 RisuAI가 준 non-model type 제외를 기준으로 하고, `model` 안에서는 저장할 최종 입력/출력 선택에 필요한 최소 검증만 남기는 방향을 검토한다.

### 3. 전체 Go 테스트 21개 실패

`go test ./... -count=1` 실측 결과:

- 고정된 `M:\risulongmemory\...` 경로: 1개
- 과거 JS 문자열/함수명/버전 marker 검사: 19개
- `/explorer/memories/regenerate`의 `code` 계약 불일치: 1개

내부 패키지 테스트 `go test ./internal/... -count=1`은 통과한다.

### 4. ChromaDB 배포 기준 혼재

- backend 기본과 관리형 실행기: `/api/v2`
- 루트 및 일부 live env: `/api/v1`
- Windows RC4: ChromaDB 1.5.9
- POSIX/Termux: `chromadb>=0.5,<1.0`
- live test: 무고정 설치

이는 실제 배포 호환성 작업이다.

### 5. Go/CI 기준 미완성

- 현재 번들 Go: 1.26.4
- 목표 보안 patch: 1.26.5
- GitHub Actions workflow 없음
- `govulncheck` 로컬 도구 없음

## 실제 3.0 남은 작업

우선순위 순서:

1. 원작 자료 자동 추출·검수 API/UI, 별도 벡터 검색, 시점 제한 recall,
   prepare-turn opt-in 주입을 `3.0-reference-work-library-and-session-binding-design.md`
   계약으로 구현
2. 안전한 백엔드 업데이트 적용 도우미, 재시작, `/ready` 확인, 자동 복구 구현
3. Windows, Ubuntu/Linux, Termux에서 기존 MariaDB·ChromaDB·`.runtime`을
   유지하는 설치·업데이트 통합 검증
4. 3.0 기능 구현 완료 후 삭제·리롤·복사·연결·이동과 현재 상태·인물
   인식 보정을 실제 장기 세션으로 통합 검증
5. 검증 결과에 따라 오류 문구와 호환 경로를 다듬고 3.0 배포 후보를 고정

## 보호 대상이며 신규 작업이 아닌 항목

다음은 현재 정상 동작하므로 3.0 신규 구현 목록에서 제외한다.

- 실제 유저 입력 저장
- Effective Input 표시
- 기존 기억 주입
- raw/derived/vector lane 저장
- 완료된 pair 중복 저장 방지
- 삭제·리롤
- 복사·연결·이동
- GigaTrans 번역 원문 선택
- 일반 후처리 최종본 저장

이 항목들은 구조 이동 시 회귀 테스트로만 사용한다.

## 최초 감사 당시 자동 검증 기록

- `node --check "Archive Center.js"`: 통과
- `go test ./internal/... -count=1`: 통과
- `go test ./... -count=1`: 실패, 위 21개 기준선과 동일

이 결과는 최초 감사 당시 기록이다. 이후 전체 회귀 묶음과 분리된 기능
테스트가 통과했다. 실제 RisuAI 훅, 원격 Oracle/Tailscale, 100턴 이상
세션은 3.0 최종 안정화 전까지 검증 완료로 주장하지 않는다.
